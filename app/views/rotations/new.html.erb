<h1>New Rotation for <%= @school.name %></h1>

<%= form_for @rotation, :url => school_rotations_path do |f| %>
  <%= f.text_field :name, :placeholder => 'Enter Rotation Name' %>
  <%= f.hidden_field :ip_address, :value => request.remote_ip %>
  <%= f.submit %>
<% end %>