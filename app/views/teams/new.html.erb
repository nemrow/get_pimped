<h1>New Team for <%= @rotation.name %></h1>

<%= form_for @team, :url => rotation_teams_path do |f| %>
  <%= f.text_field :name, :placeholder => 'Enter Team Name' %>
  <%= f.hidden_field :ip_address, :value => request.remote_ip %>
  <%= f.submit %>
<% end %>